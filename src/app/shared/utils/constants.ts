// Default US code
export const COUNTRY_CODE = 'us';
// mat icon
export enum MAT_ICON {
  CIRCLE = 'fiber_manual_record',
  MENU = 'menu',
  INFO = 'info',
}

/**
 * @description The paginator element displays a dropdown of page sizes for the user to choose from.
 * Those are the options available to choose by default
 */
export const PAGINATOR_OPTS = {
  PAGE_INDEX: 0,
  PAGE_SIZE: 10,
  PAGE_SIZES: [10, 25, 50],
};
